name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Explizite Dateiliste statt Glob: xssh hat keine .sh-Endung, und neue
      # Skripte sollen bewusst aufgenommen werden, nicht zufaellig.
      - run: shellcheck install.sh update.sh uninstall.sh
